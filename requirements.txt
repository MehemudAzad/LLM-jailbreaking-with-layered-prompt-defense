# --dry-run needs nothing outside the standard library (Python >= 3.11 for tomllib).
# tomli>=2.0 ; python_version < "3.11"        # config parsing on older Pythons

# Optional: makes core/seed.py able to seed NumPy too.
numpy>=1.26

# Dev / CI.
pytest>=8.0

# ---------------------------------------------------------------------------
# Model backend (Kaggle GPU). The notebooks install these; pin exact versions
# here once a run is reproduced end to end.
# ---------------------------------------------------------------------------
# torch                    # preinstalled on Kaggle
# transformers>=4.50       # Qwen3.5-9B (Feb 2026) needs a recent build -- notebooks use -U
# accelerate>=0.30
# bitsandbytes>=0.43       # nf4 4-bit for the Qwen3.5-9B judge (M4)
# sentencepiece
