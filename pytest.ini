[pytest]
# logs/ holds downloaded Kaggle outputs, which can include a full copy of this repo.
# Without this, pytest collects two modules named test_wiring and aborts.
testpaths = tests
norecursedirs = logs notebooks .git
